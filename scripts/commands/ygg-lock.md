Run `ygg lock list` and display the output. If arguments are provided, interpret them:
- `acquire <resource>` → `ygg lock acquire <resource> --agent $YGG_AGENT_NAME`
- `release <resource>` → `ygg lock release <resource> --agent $YGG_AGENT_NAME`
- `list` → `ygg lock list`
